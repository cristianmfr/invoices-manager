/** @typedef {import('prettier').Config} PrettierConfig */

const baseConfig = {
  trailingComma: "all",
  tabWidth: 2,
  useTabs: true,
  semi: false,
  singleQuote: true,
  jsxSingleQuote: true,
  printWidth: 80,
  arrowParens: "always",
};

export default baseConfig;
